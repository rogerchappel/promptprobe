{
  "name": "promptprobe",
  "version": "0.1.0",
  "description": "Offline linter for agent instructions, system prompts, and repo guidance files.",
  "type": "module",
  "main": "./dist/src/index.js",
  "bin": {
    "promptprobe": "./dist/src/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/tests",
    "!dist/**/*.test.*",
    "examples",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "lint": "npm run check",
    "pretest": "npm run build",
    "test": "node --test dist/tests/**/*.test.js",
    "smoke": "bash scripts/smoke.sh",
    "package:smoke": "npm run build && npm run bin:smoke && npm pack --dry-run",
    "release:check": "npm run check && npm test && npm run smoke && npm run package:smoke",
    "prepack": "npm run build",
    "bin:smoke": "node -e \"const fs=require('node:fs'); const p=require('./package.json'); const bins=typeof p.bin==='string'?{[p.name]:p.bin}:p.bin||{}; for (const [name,target] of Object.entries(bins)) { if (!fs.existsSync(target)) { console.error(name + ' bin target missing: ' + target); process.exit(1); } }\""
  },
  "keywords": [
    "agents",
    "lint",
    "prompt",
    "prompt-engineering",
    "cli",
    "markdown",
    "security"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rogerchappel/promptprobe.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/promptprobe/issues"
  },
  "homepage": "https://github.com/rogerchappel/promptprobe#readme",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
